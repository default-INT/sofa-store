import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    boxShadow: '0 5 10 0 #00000040',
    borderRadius: 16,
  },
  innerContainer: {
    padding: 16,
    gap: 16,
  },
});
